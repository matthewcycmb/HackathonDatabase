---
description: 10-minute post-hackathon retro that feeds future dossiers
argument-hint: <event-slug>
---

Run a post-event retro for: $ARGUMENTS

Interview me with AskUserQuestion (one question at a time, short): result, what won and why, what judges actually said/asked, whether the dossier's predicted angle held, hardware wins/failures/gaps, one change for next time.

Then write `events/<slug>/retro.md` from `events/_templates/retro.md`, update `inventory/parts.md` if hardware lessons apply (new parts, parts that failed), and commit ("retro: <slug>").
